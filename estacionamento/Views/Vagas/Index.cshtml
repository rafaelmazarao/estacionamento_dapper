@model IEnumerable<Vaga>

@{
    ViewData["Title"] = "Vagas";
}

<div class="text-center">
    <h1 class="display-4">Vagas</h1>
    <a class="btn btn-primary" asp-controller="Vagas" asp-action="Novo">Novo</a>       
</div>

<div class="text-center">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cod Localizacao</th>
                <th>Ocupado</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model){
                <tr>
                    <td>@item.Id</td>
                    <td>@item.CodigoLocalizacao</td>
                    <td>@(item.Ocupado ? "SIM" : "NAO")</td>
                    <td>
                        <a class="btn btn-warning" asp-route-id="@item.Id" asp-action="Editar" asp-asp-controller="Vaga" method="post">Alterar</a>                          
                    </td>
                    <td>
                        <form onsubmit="return confirm('Confirma?')" asp-route-id="@item.Id" asp-action="Apagar" asp-asp-controller="Vaga" method="post">
                            <button type="submit" class="btn btn-danger">Excluir</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
